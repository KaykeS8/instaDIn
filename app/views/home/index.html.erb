<div class="relative bg-cover bg-center h-screen" style="background-image: url(<%= asset_path('hero-image.jpg') %>)">
    <!-- Camada de escurecimento -->
    <div class="absolute inset-0 bg-black opacity-50"></div>
    <h1 class="relative pl-10 pt-4 text-white text-2xl z-10">InstaDin</h1>
    
    <!-- Conteúdo -->
    <div class="relative z-10 w-[600px] pl-10 pt-40">
        <h2 class="text-5xl font-bold text-white mb-4">Conecte-se com o que importa, rapidamente!</h2>
        <p class="text-white mb-3">Junte-se agora e faça parte dessa nova experiência de conexão instantânea!</p>
        <%= link_to 'Cadastre-se', new_user_registration_path, class: 'bg-blue-600 text-white font-semibold rounded px-4 py-2 hover:bg-blue-700 transition' %>
        <%= link_to 'entrar', new_user_session_path, class: 'border text-white font-semibold rounded px-4 py-2 hover:bg-blue-600 transition hover:border-none' %>
    </div>
</div>
